export type FakerStructure = {
  id: string;
  firstName: string;
  lastName: string;
  dateOfBirth: string;
  profilePicture: string;
};

export type ChartData = {
  month: string;
  birthdays: number;
};
